<h1>Enjoy <%= @playlist.name %></h1>

<%= link_to 'Add a new track', 'trackpicks/soundcloud', method: "post", playlist: @playlist.id %>

<% @playlist.trackpicks.each do |trackpick| %>
  <%= render partial: 'show_track', locals: {trackpick: trackpick} %>
<% end %>